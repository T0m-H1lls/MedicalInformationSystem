<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:addViewModel="clr-namespace:Medical_information_system.ViewModels.AddViewModel"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="Medical_information_system.Views.Add.AddMedicalRecord"
        x:DataType="addViewModel:AddMedicalRecordViewModel"
        Width="600"
        Height="600"
        WindowStartupLocation="CenterScreen"
        Title="Добавить запись">
    <Grid Background="Azure">
        
        <Border Classes="medCard" HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" Spacing="5">
                
                <TextBlock Text="Имя пациента"/>
                
                <StackPanel>
                <ComboBox ItemsSource="{Binding PatientsList}" Width="180" SelectedItem="{Binding SelectedPatient}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding FullName}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                </StackPanel>
                
                <StackPanel>
                    <Label Content="Диагноз"/>
                    <TextBox Text="{Binding Diagnose}"/>
                </StackPanel>
                
                <StackPanel>
                    <Label Content="Описание"/>
                    <TextBox Text="{Binding Description}"/>
                </StackPanel>
                
                <StackPanel>
                    <Label Content="Лекарство"/>
                    <TextBox Text="{Binding Medicine}"/>
                </StackPanel>
                
                <StackPanel>
                    <Label Content="Дата"/>
                    <DatePicker SelectedDate="{Binding RecordDate}"/>
                </StackPanel>
                
                <StackPanel>
                    <Label Content="Время"/>
                    <TimePicker SelectedTime="{Binding RecordTime}"
                                ClockIdentifier="24HourClock"/>
                 </StackPanel>
                
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Сохранить" Background="Green" Foreground="AliceBlue" Command="{Binding SavePrescriptionCommand}"/>
                    <Button Content="Отмена" Background ="Red" Foreground="Azure" Command="{Binding CancelCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
    
</Window>
